const SocialMedia = ({ icon }) => {
  return (
    <div className="w-9 h-9 bg-white cursor-pointer rounded-full flex items-center hover-brand justify-center">
      {icon}
    </div>
  );
};

export default SocialMedia;
